#include "SILC_Fmpi.h"
#include "config.h"

/**
 * @file  SILC_Fmpi_Ext.c
 * 
 * @brief Fortran interface wrappers for external interface functions
 */

/* uppercase defines */
#pragma wrapgen multiple restrict(gext) skel/SILC_Fmpi_DefineU.w

/* lowercase defines */
#pragma wrapgen multiple restrict(gext) skel/SILC_Fmpi_DefineL.w

/**
 * @name Fortran wrappers 
 * @{
 */

#ifndef NEED_F2C_CONV

/* If MPI_Comm_f2c is HAVE as a simple macro (which typically does nothing)
 * as it would require a "real" function if it is really needed
 * => we can save the f2c and c2s conversions */

#pragma wrapgen multiple restrict(gext+tint) skel/SILC_Fmpi_Std.w

#else /* !NEED_F2C_CONV */

#pragma wrapgen multiple restrict(gext+tint) skel/SILC_Mpi_F2c_C2f.w

#endif

/**
 * @}
 */
